import { UrlencodeDirective } from './urlencode.directive';

describe('UrlencodeDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlencodeDirective();
    expect(directive).toBeTruthy();
  });
});
